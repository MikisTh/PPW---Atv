function soma (fn) {
    if(typeof fn === 'function') {
        console.log(fn());
    };
};
function subtrair (fn) {
    if(typeof fn === 'function') {
        console.log(fn());
    };
};
function multiplicar (fn) {
    if(typeof fn === 'function') {
        console.log(fn());
    };
};
function dividir (fn) {
    if(typeof fn === 'function') {
        console.log(fn());
    };
};

function executar (fn) {
    if(typeof fn === 'function') {
        console.log(fn());
    };
};

function executar (fn, num1 = 0, num2 = 0) {
    if(typeof fn === 'function') {
        console.log(fn(num1, num2));
    };
};

